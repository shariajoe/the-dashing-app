const config = {
    // Test user credentials
    testCredentials: {
        email: 'eve.holt@reqres.in'
    },
    // API configuration
    reqres: {
        baseUrl: 'https://reqres.in/api',
        endpoints: {
            login: '/login',
            users: '/users'
        }
    },
};

export default config;